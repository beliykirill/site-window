/** @type {import('next-i18next').UserConfig} */
const nextConfig = {
    i18n: {
        defaultLocale: 'pl',
        locales: ['pl'],
        localeDetection: false,
    },
};


module.exports = nextConfig;
